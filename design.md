# 设计文档：总任务标识（批次化）+ 断点恢复

> 版本 v1（待签核）· 2026-08-14 · 目标文件 `content.js`

## 1. 目标

解决「大任务（如 90 条）跑到一半被刷新/中断后，无法只跑剩余部分」的问题。引入**总任务标识（批次 runId）**，让 `items / pending / results` 全部挂在一个批次之下，刷新后能恢复任务列表与进度，从断点继续。

## 2. 现状与痛点

- `K.items / K.pending / K.results` 是全局单槽，上传新文件即覆盖，批次间无隔离。
- `results` 只在「跳页导航」时落盘（`navigating`），**同页执行中每完成一条不落盘** → 刷新即丢进度。
- 刷新后任务列表显示全量、进度条归零、再点开始会重跑全部（靠 IN BAG 检测慢慢跳过）。

## 3. 方案设计

### 3.1 数据模型：批次结构

上传一次文件 → 生成 `runId`（`run_<时间戳36>_<随机4>`）。

- 指针键：`abw_currentRunId` → 当前批次 id
- 批次数据键（都带 runId 前缀）：
  - `run_<id>_items` 待跑任务（含已完成的，用 `_row` 对齐）
  - `run_<id>_pending` 跳页剩余任务
  - `run_<id>_results` 已完成结果
  - `run_<id>_xlHeaders` / `run_<id>_xlRows` / `run_<id>_batchNo` 原始 Excel
  - `run_<id>_logs` 运行日志（现状 `log()` 已实时落盘，仅需把键挂到批次下，刷新即不丢）
  - `run_<id>_fileName` 来源文件名
  - `run_<id>_totalCount` 本批次任务总数（进度条分母）

**改造方式**：在 `STORE` 封装层加一层「runId 前缀」解析，业务代码继续写 `K.items`，实际存储键自动带当前 runId 前缀 → 改动面最小。

### 3.2 results 实时落盘（方案成立的地基）

`runAll` 循环里每 `results.push(result)` 后立即 `STORE.set(K.results, results)`。90 条写 90 次、每次几 KB，无性能压力。

### 3.3 刷新恢复 —— **手动开始**（用户已拍板）

`createPanel` 注入时按优先级判定：

1. `pending` 非空 → 跳页中断 → **自动续跑**（现有串行导航的必需机制，保持不动，只挂 runId）。
2. `pending` 为空但 `results` 非空 → **同页执行中被刷新/中断** → 恢复列表 + 进度，**不自动跑，等用户点「开始」**。
3. 都为空 → 全新状态。

「开始加购」语义区分：有未完成批次 → 从剩余行继续（**不清空 results**）；全新文件 → 从头跑（清空 results）。

剩余行计算：`items.filter(it => !已完成行号集合.has(it._row))`，已完成行号从 `results` 提取。

### 3.4 批次显示（用户已拍板）

面板顶部显示当前批次：`📄 <文件名> · 已完成 N / 总 M`。进度条用 `results.length / totalCount`。

### 3.5 批次保留 —— 只保留当前批次（用户已拍板）

上传新文件 → 生成新 runId → 清除旧批次数据（含旧日志）→ 存新批次。避免 sessionStorage 膨胀（此前踩过撑爆的坑）。

### 3.6 日志归属（用户补充需求）

运行日志随批次绑定：`run_<id>_logs`。刷新恢复时 `restoreLogs()` 读当前批次日志；上传新文件（切新批次）时旧日志随旧批次一起清除，新批次日志从零开始。日志仍沿用 `MAX_LOGS=5000` 上限防爆仓。

## 4. 关键决策记录

| 决策点 | 结论 |
|---|---|
| 刷新后执行行为 | 手动开始（跳页导航除外，仍自动续跑） |
| 批次保留 | 只保留当前批次 |
| 批次显示 | 面板显示文件名 + 进度 |

## 5. 边界与风险

- **半成品续跑文件**：上传带「加购结果」列的文件会生成**新批次**，其 `items` 是过滤后的剩余行，但下载回写依赖该批次的 `xlRows`（全量原始行）→ 必须把 `xlHeaders/xlRows` 一并挂到批次下（已包含在 3.1）。
- **`_row` 是唯一对齐键**：过滤/恢复/回写全程用 `item._row`，不用数组下标。
- **不引入 localStorage**：刷新靠 sessionStorage 已够；关标签页/崩溃由 v1.0.9 的结果 Excel 断点续跑兜底，两层防护。

## 6. 实施顺序（供下一阶段规划）

1. STORE 层加 runId 前缀解析 + 批次工具（生成/切换/清理）。
2. `results` 实时落盘。
3. `createPanel` 注入时的批次恢复逻辑（pending 自动 / results 手动）。
4. 日志键挂批次 + `restoreLogs` 读当前批次。
5. 「开始加购」区分继续 / 全新。
6. 面板批次显示 + 上传新文件清理旧批次（含旧日志）。
7. 自测（node 模拟批次切换与恢复逻辑）+ 语法检查。
